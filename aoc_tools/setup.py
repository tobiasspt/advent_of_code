from setuptools import setup

setup(
   name='aoc_tools',
   version='0.0',
   description='My tools for solving AOC puzzles',
   author='Tobias Spitaler',
   author_email='',
   packages=['aoc_tools'],  #same as name
   install_requires=[], #external packages as dependencies
)